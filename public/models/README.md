# MediaPipe model files

Place browser-side task assets here, for example:

- `face_landmarker.task`
- optional future assets: pose/hand task models, avatar GLB/VRM

The MVP can also load MediaPipe assets from the official CDN while the local assets are prepared.
